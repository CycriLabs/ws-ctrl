/**
 * Name for the templates directory coming from the distributed NPM package
 * itself. This directory contains all templates that are used to create a new
 * workspace. It is distributed with the package.
 */
export const DIR_TEMPLATES = 'templates';

/**
 * Name for config directory. All configuration files are stored in this
 * directory, e.g. servers, use-cases, etc.
 */
export const DIR_CONFIG = 'config';
export const DIR_DATASOURCES = 'datasources';
export const DIR_DOCKER = 'docker';
export const DIR_NGINX = 'nginx';
export const DIR_POSTGRESQL = 'postgresql';
export const DIR_SECRET_TEMPLATES = 'secret-templates';
export const DIR_GIT_TEMPLATES = 'git-templates';
// entity dirs
export const DIR_REPOSITORIES = 'repositories';
export const DIR_SERVERS = 'servers';
export const DIR_USE_CASES = 'use-cases';

/**
 * Name for the directory where development files are stored. This directory is
 * used to store files that are only needed during development, e.g. module
 * repositories.
 */
export const DIR_DEVELOPMENT = 'development';
